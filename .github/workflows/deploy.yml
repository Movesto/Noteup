# name: Deploy to ECS

# on:
#   push:
#     branches: [main]

# env:
#   AWS_REGION: ${{ secrets.AWS_REGION }}
#   ECR_BACKEND:  ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/amor-backend
#   ECR_FRONTEND: ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/amor-frontend
#   ECS_CLUSTER: amor-cluster
#   BACKEND_SERVICE: amor-backend
#   FRONTEND_SERVICE: amor-frontend

# jobs:
#   # ── Lint & Test ──────────────────────────────────────────────────────────────
#   test-backend:
#     name: Backend tests
#     runs-on: ubuntu-latest
#     defaults:
#       run:
#         working-directory: backend
#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"
#           cache: pip

#       - name: Install dependencies
#         run: pip install -r requirements-dev.txt

#       - name: Run tests
#         run: python -m pytest tests/ -q --tb=short

#   test-frontend:
#     name: Frontend type-check
#     runs-on: ubuntu-latest
#     defaults:
#       run:
#         working-directory: frontend
#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: "20"
#           cache: npm
#           cache-dependency-path: frontend/package-lock.json

#       - name: Install dependencies
#         run: npm ci --legacy-peer-deps

#       - name: Type-check
#         run: npx tsc --noEmit

#   # ── Build & Push ─────────────────────────────────────────────────────────────
#   build-and-push:
#     name: Build & push images
#     runs-on: ubuntu-latest
#     needs: [test-backend, test-frontend]
#     outputs:
#       image-tag: ${{ steps.meta.outputs.tag }}

#     steps:
#       - uses: actions/checkout@v4

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region:            ${{ secrets.AWS_REGION }}

#       - name: Log in to ECR
#         id: login-ecr
#         uses: aws-actions/amazon-ecr-login@v2

#       - name: Compute image tag
#         id: meta
#         run: echo "tag=${GITHUB_SHA::8}" >> "$GITHUB_OUTPUT"

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Build & push backend
#         uses: docker/build-push-action@v5
#         with:
#           context: ./backend
#           file: ./backend/Dockerfile
#           push: true
#           tags: |
#             ${{ env.ECR_BACKEND }}:${{ steps.meta.outputs.tag }}
#             ${{ env.ECR_BACKEND }}:latest
#           cache-from: type=gha,scope=backend
#           cache-to:   type=gha,scope=backend,mode=max

#       - name: Build & push frontend
#         uses: docker/build-push-action@v5
#         with:
#           context: ./frontend
#           file: ./frontend/Dockerfile
#           push: true
#           tags: |
#             ${{ env.ECR_FRONTEND }}:${{ steps.meta.outputs.tag }}
#             ${{ env.ECR_FRONTEND }}:latest
#           cache-from: type=gha,scope=frontend
#           cache-to:   type=gha,scope=frontend,mode=max

#   # ── Deploy ───────────────────────────────────────────────────────────────────
#   deploy:
#     name: Deploy to ECS
#     runs-on: ubuntu-latest
#     needs: build-and-push
#     environment: production

#     steps:
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region:            ${{ secrets.AWS_REGION }}

#       - name: Deploy backend
#         run: |
#           IMAGE="${{ env.ECR_BACKEND }}:${{ needs.build-and-push.outputs.image-tag }}"
#           TASK_DEF=$(aws ecs describe-task-definition \
#             --task-definition amor-backend \
#             --query 'taskDefinition' --output json)

#           NEW_TASK_DEF=$(echo "$TASK_DEF" | python3 -c "
#           import json, sys
#           td = json.load(sys.stdin)
#           td['containerDefinitions'][0]['image'] = '$IMAGE'
#           for k in ['taskDefinitionArn','revision','status','requiresAttributes',
#                     'placementConstraints','compatibilities','registeredAt','registeredBy']:
#               td.pop(k, None)
#           print(json.dumps(td))
#           ")

#           NEW_ARN=$(aws ecs register-task-definition \
#             --cli-input-json "$NEW_TASK_DEF" \
#             --query 'taskDefinition.taskDefinitionArn' --output text)

#           aws ecs update-service \
#             --cluster ${{ env.ECS_CLUSTER }} \
#             --service  ${{ env.BACKEND_SERVICE }} \
#             --task-definition "$NEW_ARN" \
#             --force-new-deployment

#       - name: Deploy frontend
#         run: |
#           IMAGE="${{ env.ECR_FRONTEND }}:${{ needs.build-and-push.outputs.image-tag }}"
#           TASK_DEF=$(aws ecs describe-task-definition \
#             --task-definition amor-frontend \
#             --query 'taskDefinition' --output json)

#           NEW_TASK_DEF=$(echo "$TASK_DEF" | python3 -c "
#           import json, sys
#           td = json.load(sys.stdin)
#           td['containerDefinitions'][0]['image'] = '$IMAGE'
#           for k in ['taskDefinitionArn','revision','status','requiresAttributes',
#                     'placementConstraints','compatibilities','registeredAt','registeredBy']:
#               td.pop(k, None)
#           print(json.dumps(td))
#           ")

#           NEW_ARN=$(aws ecs register-task-definition \
#             --cli-input-json "$NEW_TASK_DEF" \
#             --query 'taskDefinition.taskDefinitionArn' --output text)

#           aws ecs update-service \
#             --cluster ${{ env.ECS_CLUSTER }} \
#             --service  ${{ env.FRONTEND_SERVICE }} \
#             --task-definition "$NEW_ARN" \
#             --force-new-deployment

#       - name: Wait for services to stabilize
#         run: |
#           echo "Waiting for backend…"
#           aws ecs wait services-stable \
#             --cluster ${{ env.ECS_CLUSTER }} \
#             --services ${{ env.BACKEND_SERVICE }}

#           echo "Waiting for frontend…"
#           aws ecs wait services-stable \
#             --cluster ${{ env.ECS_CLUSTER }} \
#             --services ${{ env.FRONTEND_SERVICE }}

#       - name: Deployment summary
#         run: |
#           echo "### Deployment complete" >> "$GITHUB_STEP_SUMMARY"
#           echo "- **Tag**: \`${{ needs.build-and-push.outputs.image-tag }}\`" >> "$GITHUB_STEP_SUMMARY"
#           echo "- **Backend**: \`${{ env.ECR_BACKEND }}:${{ needs.build-and-push.outputs.image-tag }}\`" >> "$GITHUB_STEP_SUMMARY"
#           echo "- **Frontend**: \`${{ env.ECR_FRONTEND }}:${{ needs.build-and-push.outputs.image-tag }}\`" >> "$GITHUB_STEP_SUMMARY"

import Highlight from "@tiptap/extension-highlight";

// multicolor: true allows each mark to carry its own background-color inline style
export const HighlightExtension = Highlight.configure({ multicolor: true });

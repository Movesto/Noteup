# Netdata go.d Prometheus collector — pulls the backend's FastAPI metrics
# (exposed at /metrics by prometheus-fastapi-instrumentator) into Netdata.
jobs:
  - name: noteup-backend
    url: http://backend:8000/metrics
